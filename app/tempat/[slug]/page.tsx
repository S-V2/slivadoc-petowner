import type { Metadata } from "next";
import Link from "next/link";
import { notFound } from "next/navigation";
import JsonLd from "../../components/seo/JsonLd";
import { Breadcrumbs, PublicPage } from "../../components/seo/PublicSite";
import { getPublicPlace, placeSchemaType } from "../../lib/public-directory";
import { absoluteUrl, breadcrumbSchema, pageMetadata } from "../../lib/seo-config";

export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const{slug}=await params;const place=await getPublicPlace(slug);if(!place)return pageMetadata({title:"Tempat tidak ditemukan",description:"Profil mitra Slivadoc tidak ditemukan atau sudah tidak aktif.",path:`/tempat/${slug}`,noIndex:true});const title=`${place.name} ${place.branchName} — Layanan Pet di ${place.city}`;const description=`Lihat alamat dan ${place.services.length} layanan ${place.name} cabang ${place.branchName} di ${place.city} melalui direktori Slivadoc.`;return pageMetadata({title,description,path:`/tempat/${place.slug}`,keywords:[`${place.name} ${place.city}`,`pet care ${place.city}`,`dokter hewan ${place.city}`,`petshop ${place.city}`]});}

export default async function PlaceDetailPage({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const place=await getPublicPlace(slug);if(!place)notFound();const type=placeSchemaType(place);const schema=[{"@context":"https://schema.org","@type":type,"@id":absoluteUrl(`/tempat/${place.slug}#business`),name:place.name,department:place.branchName,url:absoluteUrl(`/tempat/${place.slug}`),address:{"@type":"PostalAddress",streetAddress:place.address,addressLocality:place.city,addressCountry:"ID"},...(typeof place.latitude==="number"&&typeof place.longitude==="number"?{geo:{"@type":"GeoCoordinates",latitude:place.latitude,longitude:place.longitude}}:{}),...(typeof place.rating==="number"&&place.rating>0&&typeof place.reviewCount==="number"&&place.reviewCount>0?{aggregateRating:{"@type":"AggregateRating",ratingValue:place.rating,reviewCount:place.reviewCount}}:{}),hasOfferCatalog:{"@type":"OfferCatalog",name:`Layanan ${place.name}`,itemListElement:place.services.map((service)=>({"@type":"Offer",price:service.price,priceCurrency:"IDR",itemOffered:{"@type":"Service",name:service.name,serviceType:service.category}}))}},breadcrumbSchema([{name:"Beranda",path:"/"},{name:"Direktori tempat",path:"/tempat"},{name:place.name,path:`/tempat/${place.slug}`}])];return <PublicPage><JsonLd data={schema}/><section className="seo-hero"><Breadcrumbs items={[{label:"Beranda",href:"/"},{label:"Direktori tempat",href:"/tempat"},{label:place.name}]}/><div className="seo-hero-grid"><div><span className="seo-eyebrow">Mitra Slivadoc · {place.city}</span><h1>{place.name} — {place.branchName}</h1><p>{place.address}. Lihat layanan yang dipublikasikan dari sistem mitra dan lanjutkan pencarian atau booking melalui Slivadoc.</p><div className="seo-hero-actions"><Link className="seo-primary" href="/?view=discover">Buka di Slivadoc</Link><Link className="seo-secondary" href="/tempat">Kembali ke direktori</Link></div>{place.latitude!=null&&place.longitude!=null&&<div className="seo-hero-actions"><a className="seo-secondary" href={`https://www.google.com/maps/dir/?api=1&destination=${place.latitude},${place.longitude}`} target="_blank" rel="noreferrer">Petunjuk arah</a></div>}</div><aside className="seo-hero-panel"><strong>Informasi lokasi</strong><ul><li>{place.address}</li><li>{place.city}</li><li>{place.services.length} layanan aktif</li>{place.rating!=null&&place.rating>0&&<li>Rating {place.rating} dari {place.reviewCount??0} ulasan</li>}</ul></aside></div></section><section className="seo-main-section"><div className="seo-section-heading"><h2>Layanan yang tersedia</h2><p>Harga dan ketersediaan mengikuti data aktif dari sistem mitra pada saat halaman dimuat.</p></div><div className="seo-card-grid">{place.services.map((service)=><article className="seo-card" key={service.id}><small>{service.category}</small><h2>{service.name}</h2><p>{service.durationMinutes>0?`${service.durationMinutes} menit · `:""}{service.price>0?new Intl.NumberFormat("id-ID",{style:"currency",currency:"IDR",maximumFractionDigits:0}).format(service.price):"Hubungi mitra"}</p></article>)}</div></section></PublicPage>}
